# Chapter 3. 컨테이너 구성과 애플리케이션 설계

## 학습 목표

1. 컨테이너 구성과 실행 방법
2. 이식성을 고려한 애플리케이션 생성 방법
3. 자격 증명(보안 정보) 다루는 방법
4. 영속화 대상 데이터를 다루는 방법

---

## 1. 컨테이너 설계 원칙

> 서드파티 레지스트리에 공개된 애플리케이션 이미지와 미들웨어 이미지를 함께 조합해서 쓰는 경우가 많아지면서, "애플리케이션을 컨테이너에 어떻게 배치할 것인가"가 중요한 설계 문제가 되었다. 컨테이너의 역할은 애플리케이션과 파일시스템을 컨테이너 단위로 분리해두는 것.

### 방법1 — 하나의 컨테이너 = 하나의 프로세스 (엄격한 원칙)

정기적으로 잡(job)을 실행해야 하지만 자체 스케줄러 기능은 없는 애플리케이션이 대상이다. 크론 컨테이너와 잡 컨테이너를 분리해서, 잡 컨테이너에 실행 API를 준비해두고 크론 컨테이너가 컨테이너 간 통신으로 그 API를 호출하거나, 크론 컨테이너 안에 도커를 설치해서 잡 컨테이너를 직접 실행(DinD, Docker in Docker)하는 방식이다.

- **잡(job)**: 정해진 시각·주기에 실행되어야 할 작업 단위
- **스케줄러(cron)**: 리눅스/유닉스 계열 OS에 내장된 시간 기반 작업 스케줄러. 백그라운드에서 계속 켜져 있는 데몬

### 방법2 — 하나의 컨테이너 = 하나의 관심사 (도커 공식 권장 원칙)

> 도커 공식 문서 원칙: 컨테이너는 하나의 관심사(concern)에만 집중해야 한다.

방법1처럼 프로세스 단위로 엄격히 쪼개면 오히려 불필요한 복잡함이 생길 수 있다 — "1분마다 트리거하는 크론"과 "Hello!를 출력하는 잡"을 억지로 나누기보다, "1분마다 Hello!를 출력한다"는 하나의 관심사로 통합하는 게 더 적절한 경우가 있다. 핵심은 프로세스 개수가 아니라, 각 컨테이너가 명확한 역할·도메인 하나를 맡고 있는지, 그리고 복제되어도 부작용 없이 동일하게 동작하는 스택인지다.

### 실습 — 한 컨테이너에서 cron + task.sh 함께 실행

```
~/work/ch03/cronjob
├── Dockerfile
├── cron-example   (크론 정의 파일, task.sh를 매분 실행)
└── task.sh        (일정 간격으로 "Hello!" 출력)
```

```dockerfile
FROM ubuntu:24.04

RUN apt update
RUN apt install -y cron

COPY task.sh /usr/local/bin/
COPY cron-example /etc/cron.d/
RUN chmod 0644 /etc/cron.d/cron-example

CMD ["cron", "-f"]
```

| 명령 | 설명 |
| --- | --- |
| `COPY task.sh /usr/local/bin/` | 스크립트를 이미지 안의 `/usr/local/bin/`(실행 파일들이 모이는 표준 경로)로 복사 |
| `COPY cron-example /etc/cron.d/` | job 정의 파일을 cron이 자동으로 읽는 표준 경로로 복사. cron은 이 디렉터리 안 파일들을 알아서 job 목록에 등록함 |
| `RUN chmod 0644 /etc/cron.d/cron-example` | job 정의 파일 권한을 644로 맞춤. cron은 이 파일 권한이 너무 느슨하면(다른 사용자도 쓰기 가능) 보안상 job으로 인식을 거부하기 때문에 명시적으로 정확한 권한을 지정 |
| `CMD ["cron", "-f"]` | 컨테이너 시작 시 cron을 포어그라운드(-f)로 실행. `-f` 없이 백그라운드로 돌리면 1번 프로세스가 곧바로 끝난 것처럼 보여서 컨테이너가 즉시 종료돼버림 |

```
# cron-example
* * * * * root sh /usr/local/bin/task.sh > /proc/1/fd/1 2>&1
```

`/proc/1/fd/1`은 컨테이너 PID 1(cron -f)의 표준출력을 가리키는 경로 — 여기로 리다이렉트해야 `docker logs`에 job 실행 결과가 잡힌다.

```powershell
docker image build -t ch03/cronjob:latest .
docker container run -d --rm --name cronjob ch03/cronjob:latest
docker container logs -f cronjob
```

> ⚠️ **주의**: cron-example 파일이 CRLF 줄바꿈(Windows 에디터 저장 시 흔함)으로 저장되면 cron이 파싱에 실패해 조용히 job을 무시한다. `docker exec -it cronjob cat -A /etc/cron.d/cron-example`로 줄 끝에 `^M$`이 보이면 CRLF가 섞인 것 — LF로 저장해서 재빌드해야 한다.

---

## 2. 컨테이너 이식성의 한계 — 완벽할 수 없는 예외

### 예외1 — 커널 및 아키텍처 차이

컨테이너는 자체 커널이 없고 호스트 OS의 커널을 공유하는 방식이라, 실행 가능한 호스트는 특정 CPU 아키텍처·OS를 전제로 성립한다(리눅스용 이미지는 리눅스 커널 호스트에서만 직접 실행 가능).

이를 완화하기 위해 등장한 게 **BuildKit**(정확히는 BuildKit 위에서 동작하는 **buildx**) — 기존 `docker image build`를 확장해서 멀티 플랫폼 이미지를 한 번에 빌드할 수 있다. 하나의 이미지 이름 아래 여러 아키텍처 버전을 묶어두면, pull 시 도커가 호스트에 맞는 버전을 알아서 골라준다.

### 예외2 — 라이브러리 / 동적 링크

컨테이너 밖에서는 잘 도는 애플리케이션이 컨테이너 안에서는 필요한 라이브러리가 없어 동작하지 않는 사례가 있다.

| 구분 | 설명 |
| --- | --- |
| 정적 링크(static link) | 필요한 라이브러리 코드를 실행 파일 안에 통째로 포함 → 의존성 없이 어디서든 실행 가능 |
| 동적 링크(dynamic link) | 실행 파일엔 참조만 있고, 실제 라이브러리는 실행 시점(런타임)에 찾아 연결 → 환경에 라이브러리가 없거나 버전이 다르면 실패 |

Dockerfile의 ADD/COPY로 컨테이너 밖에서 만든 애플리케이션을 그대로 가져오는 경우도 있는데, 대표적으로 **CI에서 빌드·테스트 후 이미지에 패키징**하는 패턴이다. 이 경우 이식성 문제는 **동적 링크를 사용하는 애플리케이션의 컨테이너에서만** 발생한다 (정적 링크면 문제없음).

> ⚙️ **CI(Continuous Integration)**: 코드를 저장소에 올릴 때마다 자동으로 빌드·테스트해주는 프로세스·도구. 코드 push → CI 서버가 감지 → 빌드·테스트 실행 → 문제없으면 이미지 빌드·레지스트리 push, 실패하면 알림.

**대응 원칙**: 컨테이너에서 실행할 애플리케이션은 가능하면 네이티브 라이브러리를 정적 링크로 빌드하는 걸 먼저 고려한다. 다만 바이너리 크기가 커지는 단점이 있어 **멀티스테이지 빌드(Multi-stage builds)**로 완화한다.

---

## 3. 컨테이너 친화적인 애플리케이션 — 설정값 다루기

- 필요 요소1: 설정 파일을 이미지에 포함해서 빌드
- 필요 요소2: 설정을 컨테이너 외부에서 주입
  - 2-1: 커맨드라인 인수로 전달
  - 2-2: 환경 변수로 전달
  - 2-3: 설정 파일을 전달(마운트 등)

같은 이미지를 여러 환경(개발/스테이징/운영)에서 재사용하려면 설정은 이미지에 굽지 말고 실행 시점에 외부에서 주입하는 게 유리하다.

---

## 4. 보안 정보(자격 증명) 관리

> 🔐 실제로 컨테이너를 운영할 때는 애플리케이션 로직 외에도 훨씬 많은 설정값을 다루게 되는데, 그중에서도 비밀번호·API 키·인증서 같은 보안 정보는 제3자에게 유출되지 않도록 특별히 관리해야 한다.

### 방법1) 보안 정보를 받는 컨테이너

이미지 자체엔 비밀값을 담지 않고 컨테이너를 실행하는 시점에 외부에서 주입받는 방식이다.

| 방식 | 설명 |
| --- | --- |
| 커맨드라인 인수로 전달 | `docker run 이미지 --password=xxx`. 단점: `docker inspect`나 프로세스 목록(`ps`)에서 그대로 노출될 위험 |
| 환경 변수로 전달 | `docker run -e DB_PASSWORD=xxx`. 이미지엔 안 남지만, `docker inspect`의 `Config.Env`에 그대로 노출되고 로그에 실수로 찍히거나 자식 프로세스에 상속되는 문제 |
| 설정 파일을 전달 | 볼륨 마운트로 값이 담긴 파일 자체를 컨테이너 안에 넣어줌. 값이 프로세스 목록이나 `docker inspect`에 직접 드러나지 않아 앞의 두 방식보다 안전 |

### 방법2) 버전 관리 시스템(VCS)과 보안 정보 관리의 어려움

compose.yaml 같은 설정 파일에 비밀번호를 직접 적어두면, 그 파일을 Git 같은 버전 관리 시스템에 커밋하는 순간 커밋 이력에 영구히 남는다 — 나중에 파일에서 지워도 과거 커밋 기록엔 그대로 남아있어서 사실상 완전한 제거가 불가능하다.

- 설정을 코드와 함께 버전 관리 → 편리하지만 보안 정보가 새어나감
- 보안 정보를 버전 관리에서 제외 → 안전하지만 협업·재현이 어려워짐

### 방법3) 시크릿을 사용한 보안 정보 관리

**핵심 아이디어**: 비밀번호를 compose.yaml에 직접 적지 말고, 별도 파일에 두고 그 파일을 컨테이너에 마운트한다 — VCS엔 "이 경로의 파일을 참조한다"는 구조만 올라가고, 실제 비밀번호 파일은 `.gitignore`로 제외함으로써 방법2의 딜레마를 해결한다.

```yaml
# ~/work/ch03/mysql-secrets/compose.yaml
version: "3.9"
services:
  mysql:
    container_name: mysql
    image: mysql:8.0.33
    environment:
      MYSQL_ROOT_PASSWORD_FILE: /run/secrets/mysql_root_password
      MYSQL_DATABASE: test
    secrets:
      - mysql_root_password
secrets:
  mysql_root_password:
    file: ./mysql_root_password
```

| 항목 | 역할 |
| --- | --- |
| 최상위 `secrets` | 시크릿 정의. `file:`로 비밀번호가 담긴 실제 파일 지정 |
| 서비스의 `secrets` | 이 컨테이너에서 해당 시크릿을 쓰겠다는 선언 (배열이라 여러 개 가능) |
| `MYSQL_ROOT_PASSWORD_FILE` | 값이 아니라 파일 경로를 받는 환경 변수 — 방법1의 "환경 변수 방식"과 다르게 값 자체가 아니라 파일을 가리키기만 하므로 `docker inspect`에 비밀값이 직접 노출되지 않음 |

시크릿은 컨테이너 내부 `/run/secrets`에 파일로 마운트된다:

```bash
docker container exec -it mysql bash
ls -l /run/secrets
# -rw-r--r-- 1 root root 18 Jun 26 04:34 mysql_root_password
```

> 📦 **[칼럼] 소프트웨어 공급망 공격** — 2021년 Codecov 사고: CI 환경에서 테스트 커버리지를 전송하는 Bash Uploader 스크립트가 변조되어, 이를 쓰던 수많은 프로젝트의 보안 정보가 유출됐다. 시크릿을 아무리 잘 관리해도, 내가 쓰는 서드파티 도구가 뚫리면 내 자격 증명도 함께 털린다는 사례.

### 완벽한 대책은 존재하지 않는다

보안 정보를 완벽히 지키는 방법은 없다는 전제 위에서, 다음 구조를 갖추는 게 중요하다.

- 보안 정보의 부정 사용을 감지하는 구조 (로그 감시 및 이상 탐지)
- 신속하게 자격 증명을 무효화하고 로테이션할 수 있는 구조
- 만료 시간이 짧은 임시 자격 증명을 사용하는 구조 (OIDC 등)

---

## 5. 영속성 데이터의 사용 방법

컨테이너 실행 중 기록된 파일은 컨테이너를 파기하면 함께 사라진다. 무상태(stateless) 애플리케이션은 상관없지만, DB처럼 데이터가 남아야 하는 상태 유지(stateful) 컨테이너는 별도 대책이 필요하다 — 이게 영속성 데이터(persistent data) 문제다.

### Data Volume

컨테이너 내부 디렉터리를 호스트에 저장하기 위한 구조다.

```
docker container run [options] -v 호스트디렉터리:컨테이너디렉터리[:태그] [커맨드] [커맨드 인수]
```

```bash
docker container run -v ${PWD}:/var/local/ch03 -it ubuntu:24.04 bash
root@1a009911ad65:/# echo "Volume test" > /var/local/ch03/text.txt
root@1a009911ad65:/# exit
$ cat text.txt
Volume test
```

컨테이너 안에서 만든 파일이 호스트에 그대로 남고, 이미지를 교체해도 데이터는 계속 사용 가능하다. 단점은 호스트의 특정 경로에 의존한다는 것 — 호스트가 바뀌면 깨진다.

### Data Volume 컨테이너

호스트 디렉터리에 직접 붙지 않고, Volume 전용 컨테이너를 하나 만들어 그것과 공유하는 방식이다.

| 구분 | Volume 공유 (`-v`) | Data Volume 컨테이너 (`--volumes-from`) |
| --- | --- | --- |
| 저장 위치 | 호스트의 임의 경로 | 도커 관리 영역 (`/var/lib/docker/volumes/[volume-id]`) |
| 의존성 | 호스트의 특정 경로에 의존 | 도커가 관리 → 호스트 구조에 영향 안 받음 |

```dockerfile
# ~/work/ch03/mysql-volume/Dockerfile
FROM busybox
VOLUME /var/lib/mysql
CMD ["bin/true"]
```

```powershell
docker image build -t ch03/mysql-data:latest .
docker container run -d --name mysql-data ch03/mysql-data:latest
```

`bin/true`만 실행하므로 컨테이너는 실행 직후 바로 종료된다 (정상 — Volume만 남기면 되니까).

```powershell
docker container run -d --rm --name mysql \
  -e "MYSQL_ALLOW_EMPTY_PASSWORD=yes" \
  -e "MYSQL_DATABASE=volume_test" \
  --volumes-from mysql-data \
  mysql:8.0.33
```

`--volumes-from` : 다른 컨테이너에 마운트된 Volume을 이 컨테이너에도 마운트.

```bash
docker container exec -it mysql mysql -u root -p volume_test
# CREATE TABLE user(...); INSERT INTO user ...;
docker container stop mysql
# 새 컨테이너를 같은 --volumes-from으로 다시 실행
docker container exec -it mysql mysql -u root -p volume_test
mysql> SELECT * FROM user;
# 3 rows in set  ← 데이터 살아있음
```

Data Volume 컨테이너를 분리해두면 데이터와 컨테이너의 교체가 쉬워진다.

```yaml
version: "3.9"
services:
  mysql:
    container_name: mysql
    image: mysql:8.0.33
    environment:
      MYSQL_ALLOW_EMPTY_PASSWORD: yes
      MYSQL_DATABASE: volume_test
    volumes_from:
      - mysql_data
  mysql_data:
    container_name: mysql-data
    build: .
```

### 데이터의 내보내기와 복원하기

Data Volume 컨테이너는 동일한 컨테이너 호스트 안에서만 유효하다. 다른 호스트로 옮기려면 직접 꺼내야 한다.

```bash
docker container run -v ${PWD}:/tmp \
  --volumes-from mysql-data \
  busybox \
  tar cvzf /tmp/mysql-backup.tar.gz /var/lib/mysql
```

busybox를 새로 띄우고 `--volumes-from`으로 데이터에 접근 → `tar`로 아카이브 → `/tmp`를 호스트 현재 디렉터리에 마운트해뒀으니 호스트로 파일이 떨어진다. 복원은 이 아카이브를 푼 Data Volume 컨테이너를 새로 만들면 된다.

> ⚠️ `docker image save`는 컨테이너 이미지를 아카이브하는 기능이라 Data Volume엔 적용되지 않는다.

컨테이너는 애플리케이션 이식성은 뛰어나지만, 여러 호스트를 넘나드는 데이터 이식성은 표준 기능만으로는 꽤 어렵다. 이 문제는 쿠버네티스로 대표되는 컨테이너 오케스트레이션 기술로 해결한다.
