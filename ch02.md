# Chapter 2. 컨테이너 배포

> 🚀 Chapter 1이 "도커란 무엇인가"를 다뤘다면, Chapter 2는 "컨테이너를 실제로 만들고, 실행하고, 외부에 노출시키는 방법"을 다룬다. 아래 개념들을 실제로 손으로 쳐본 기록은 `practice-log.md`에 정리되어 있다.

## 1. 컨테이너와 컨테이너 이미지

| 구분 | 정의 |
| --- | --- |
| 컨테이너 이미지 | 컨테이너를 구성하는 파일 시스템, 실행 애플리케이션, 설정을 모아둔 것. 컨테이너를 생성하기 위한 템플릿 |
| 컨테이너 | 컨테이너 이미지를 기반으로 생성되어, 파일 시스템과 애플리케이션이 실제로 실행되고 있는 상태 |

하나의 컨테이너 이미지로 여러 개의 컨테이너를 생성할 수 있다. 이미지를 다루는 기초(컨테이너 레지스트리, 이미지 지정, 이미지 pull/실행)는 Chapter 1 및 실습 로그에서 이미 다룬 내용과 같다.

---

## 2. Dockerfile Instruction 상세

| 명령어 | 지속 범위 | 설명 |
| --- | --- | --- |
| WORKDIR | 빌드 + 실행 시 유지 | 컨테이너 내 현재 디렉터리를 지정. 해당 경로가 없으면 새로 생성됨 |
| LABEL | 이미지에 고정 | 이미지 생성자, 버전, 소스 URL 등 메타데이터 기입 용도 |
| ENV | 빌드 + 실행 시 유지 | 이 Dockerfile로 만든 컨테이너 안에서 계속 사용할 수 있는 환경변수 지정. 실행 중인 컨테이너 안에서도 계속 조회 가능 |
| ARG | 빌드 중에만 (임시) | 빌드 시점에 외부에서 값을 주입받기 위한 임시 변수. 빌드가 끝나면 사라지므로 실행 중인 컨테이너 안에서는 조회되지 않음 (ENV와의 핵심 차이) |

### CMD 지정 방식 3가지

| 방식 | 문법 | 특징 |
| --- | --- | --- |
| 1. Shell 형식 | `CMD echo "Hello"` | 내부적으로 `/bin/sh -c`를 거쳐 실행됨. 사람이 터미널에 치듯이 그대로 적는 방식 |
| 2. Exec 형식 | `CMD ["실행파일", "인수1", "인수2"]` | 셸을 거치지 않고 프로세스를 직접 실행. Docker 공식 권장 방식 (JSON 배열 표기) |
| 3. ENTRYPOINT의 기본 인수로 지정 | `ENTRYPOINT ["go"]` / `CMD ["version"]` | ENTRYPOINT는 고정, CMD는 그 뒤에 붙는 "기본 인수"로만 동작. `docker run` 시 인수를 주면 CMD 부분만 교체됨 |

Dockerfile에 CMD가 여러 줄 있으면 **마지막 하나만 유효**하다.

---

## 3. 이미지 빌드하기

```powershell
docker image build -t 이미지명[:태그명] Dockerfile의_디렉터리_경로
```

| 요소 | 설명 |
| --- | --- |
| -t 옵션 | 임의의 이미지명을 지정. 생략하면 이미지명 없이 해시값(이미지 ID)으로만 관리해야 하므로, -t 옵션과 이미지명은 반드시 지정하는 것이 좋음 |
| 태그명 생략 시 | 자동으로 latest 태그가 적용됨 |
| 마지막 인수 (경로) | Dockerfile이 있는 디렉터리 경로. 현재 디렉터리(PowerShell 프롬프트에 `PS C:\...>` 형태로 표시되는, 지금 셸이 위치한 경로)와 Dockerfile 위치가 같다면 `.`으로 지정 |

> 💡 **Q&A**: "현재 디렉터리"는 파워쉘 프롬프트 앞에 뜨는 그 경로(cwd)가 맞다. `docker image build`의 `.`은 "지금 프롬프트가 서 있는 폴더를 빌드 컨텍스트로 써라"는 뜻이며, Dockerfile도 기본적으로 이 폴더 안에서 찾는다. "Dockerfile: no such file" 에러는 전부 프롬프트가 엉뚱한 폴더에 서 있는 상태에서 빌드를 실행했기 때문이었다.

---

## 4. 컨테이너 실행하기

| 명령 | 동작 |
| --- | --- |
| `docker container run -it ch02/echo:latest` | 포어그라운드에서 동작. 터미널이 컨테이너에 붙잡히며, `Ctrl+C`로 종료하면 컨테이너도 같이 멈춤 |
| `docker container run -d ch02/echo:latest` | 백그라운드에서 컨테이너 실행. 터미널에는 컨테이너 ID(해시값 문자열)만 출력되고 바로 프롬프트로 복귀 |

---

## 5. 포트 포워딩

컨테이너 안의 echo 앱은 8080번 포트를 열고 있지만, 컨테이너는 자기만의 격리된 네트워크 공간을 가지므로 호스트에서 그 포트로 바로 접속할 수 없다. 외부 요청을 컨테이너 내부로 전달해주는 것이 포트 포워딩이다.

```powershell
curl http://localhost:8080/   # 포트 매핑 없이 실행한 상태 → 접속 불가
```

```powershell
docker container stop 컨테이너ID
docker container run -d -p 9000:8080 ch02/echo:latest
```

| 위치 | 값 (예: 9000:8080) | 의미 |
| --- | --- | --- |
| 왼쪽 | 9000 | 호스트(내 컴퓨터)에서 열리는 포트. 외부에서 실제로 접속하는 포트 |
| 오른쪽 | 8080 | 컨테이너 내부 포트. 앱 코드가 실제로 열어놓은 포트 |

```powershell
curl http://localhost:9000/    # 호스트 9000 → 컨테이너 8080으로 전달됨 → 성공
```

주의: 매핑 이후에도 `localhost:8080`으로는 여전히 접속되지 않는다 — 8080은 컨테이너 내부에만 열려 있고, 호스트에 실제로 열린 문은 왼쪽 숫자(9000)이기 때문이다.

---

## 6. Docker Compose (도커 컴포즈)

### 배경 — 웹 애플리케이션 구성과 프록시

일반적인 웹 애플리케이션은 **웹서버 + 웹 애플리케이션 서버(WAS) + 데이터베이스**로 구성된다. 여기서 웹서버는 **리버스 프록시(reverse proxy)** 역할을 한다.

> 🔀 **프록시(Proxy)**: 네트워크에서 중개 역할을 수행하는 서버 또는 프로그램. 클라이언트와 서버가 직접 연결되는 대신, 프록시가 중간에서 중계 역할을 수행한다.

**프록시의 장점 4가지**

| 장점 | 내용 |
| --- | --- |
| 익명성 제공 | 클라이언트 IP 주소를 숨기고, 프록시 서버의 IP 주소로 서버에 접속 가능 |
| 캐싱 | 자주 요청되는 웹 페이지나 데이터를 캐싱해두어 같은 요청 시 빠른 응답 가능 → 네트워크 트래픽 감소, 응답속도 향상 |
| 보안 및 로깅 | 네트워크 트래픽을 모니터링하고, 보안정책 적용 및 데이터 로그 기록 가능 |
| 로드 밸런싱 | 여러 서버가 있을 때 프록시가 클라이언트 요청을 적절히 분배해 각 서버에 고르게 부하가 걸리도록 함 → 서버 성능 최적화 |

**Forward Proxy vs Reverse Proxy**

프록시는 구조적 위치, 즉 어느 방향으로 데이터를 제공하느냐에 따라 구분된다.

| 구분 | 위치 | 역할 |
| --- | --- | --- |
| Forward Proxy | 클라이언트(사용자) 앞 | 사용자의 요청을 서버에 대신 전달 → 클라이언트를 보호하고 사용자의 익명성을 유지 (특정 웹사이트 접근 제한, 콘텐츠 필터링에 활용) |
| Reverse Proxy | 웹 서버 앞 | 서버로 향하는 요청을 대신 처리 → 서버를 보호. 사용자는 프록시의 존재를 인지하지 못하고 바로 서비스를 받는 것으로 인지 (여러 서버로 요청을 분산하는 로드밸런싱, 서버 대신 복잡한 SSL 암호화 처리 수행) |

> 🔒 **SSL(Secure Sockets Layer)**: 웹사이트와 사용자 간의 통신을 암호화해 보안을 유지하는, HTTP 프로토콜의 기반 기술.

이런 구성을 배경으로, 여러 애플리케이션이 상호작용하며 하나의 시스템을 이룬다. 도커에서는 이 역할을 '컨테이너'가 수행하며, 복수의 컨테이너가 서로 통신하고 의존성을 가진다.

N개의 컨테이너를 실행하려면 각 컨테이너의 동작을 제어하는 과정에서 설정 파일이나 환경 변수 같은 여러 요소를 함께 고려해야 하는데, 이 문제를 해결하기 위해 등장한 것이 **도커 컴포즈**다.

### Docker Compose 기본 — 단일 컨테이너 실행

YAML 형식의 설정 파일 하나로 여러 컨테이너 실행을 한 번에 관리한다.

```yaml
# compose.yaml
version: "3.9"
services:
  echo:
    image: ghcr.io/jpubdocker/echo:v0.1.0
    ports:
      - 9000:8080
```

- `services` 내부의 `echo` : 컨테이너(서비스) 이름 정의
- `echo` 내부의 `image` : 실행할 컨테이너 이미지 지정
- `echo` 내부의 `ports` : 포트 포워딩 설정

`compose.yaml`이 존재하는 디렉터리에서, 정의한 내용을 기반으로 컨테이너 그룹이 실행된다.

```powershell
docker compose up -d      # -d : 백그라운드에서 실행
docker compose down       # 정의된 컨테이너를 모두 중지·제거
```

이미 존재하는 컨테이너 이미지뿐 아니라, 이미지 빌드도 함께 진행할 수 있다 — 빌드된 이미지를 그대로 실행 가능.

```yaml
# compose.yaml (수정 — image 대신 build)
version: "3.9"
services:
  echo:
    build: .
    ports:
      - 9000:8080
```

`image` 속성 대신 `build` 속성을 사용하면, Dockerfile이 존재하는 디렉터리의 상대 경로를 지정하는 방식이 된다 (`.`은 같은 계층, 즉 현재 디렉터리를 뜻함).

```powershell
docker compose up -d --build
```

`--build` : 재빌드가 필요 없어 보이는 상황에서도 빌드 과정을 반드시 수행하게 함. 이 옵션이 없으면 기존에 빌드된 이미지가 있을 경우 빌드를 생략해버림.

### 여러 컨테이너 실행하기 — echo + nginx 리버스 프록시 구성

예제로 echo 서버 앞단에 **nginx**(높은 성능·안정성으로 가장 널리 쓰이는 웹서버)를 리버스 프록시로 배치하는 구성을 만들어본다.

**1) 작업 디렉터리 준비**

```yaml
# compose.yaml
version: "3.9"
services:
  echo:
    image: ghcr.io/jpubdocker/echo:v0.1.0
```

nginx에서 echo 컨테이너로 컨테이너 간 통신을 하므로, echo 쪽엔 포트 포워딩 설정이 필요 없다.

**2) nginx 컨테이너 구조 파악 — 리버스 프록시 역할**

```powershell
docker container run -d --name nginx --rm nginx:1.25.1
docker container exec -it nginx bash
```

nginx 설정 파일의 루트 디렉터리는 `/etc/nginx`.

`/etc/nginx/nginx.conf`(기본 설정 파일) 안의 `include /etc/nginx/conf.d/*.conf;`는 `conf.d` 디렉터리 안의 `.conf` 확장자 파일을 모두 로드한다는 의미 — 여기에 nginx의 디폴트 페이지를 반환하기 위한 설정 파일이 이미 들어있다.

리버스 프록시 설정 파일은 이 `/etc/nginx/conf.d` 디렉터리에 배치하는 게 권장 경로다. 설정 파일은 이미지 빌드 시 컨테이너 안의 이 디렉터리로 복사된다. 로컬 작업 디렉터리에도 같은 구조(`nginx/etc/nginx/conf.d`)를 미리 만들어둔다.

```
.
├─ nginx
│   └─ etc
│       └─ nginx
│           └─ conf.d
└─ compose.yaml
```

**3) upstream.conf — 프록시로 넘길 대상 서버 정의**

위치: `./nginx/etc/nginx/conf.d/upstream.conf`

```nginx
upstream echo {
    server echo:8080 max_fails=3 fail_timeout=10s;
}
```

- `upstream` : nginx 문법에서 "프록시로 넘길 대상 서버들의 묶음"에 이름을 붙이는 블록. 여기선 `echo`라는 이름으로 묶음을 만들고, 그 안에 실제 대상 서버를 `server echo:8080`으로 지정
- `echo:8080`의 `echo` : IP 주소가 아니라 `compose.yaml`에 정의할 서비스 이름. 나중에 echo 컨테이너를 컴포즈로 같이 띄우면, nginx 컨테이너 안에서 `echo`라는 이름만으로 그 컨테이너를 찾아갈 수 있음(컴포즈가 서비스 이름 기반으로 자동 연결해주는 이름 해석 기능)
- `8080` : echo 컨테이너 안에서 앱이 듣고 있는 포트 (`main.go`의 `:8080`)
- `max_fails=3 fail_timeout=10s` : 이 서버가 10초 동안 3번 연속 실패하면 "죽었다"고 판단하고, 그 10초 동안 요청 대상에서 잠깐 제외하는 상태 체크 설정

**4) echo.conf — 가상 호스트 설정**

위치: `./nginx/etc/nginx/conf.d/echo.conf`

특정 조건(포트, 도메인)으로 들어온 요청을 어떻게 처리할지 정의하는 파일.

```nginx
server {
  listen 80;
  server_name echo.jpub.local;
  location / {
    proxy_pass http://echo;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $remote_addr;
    access_log /dev/stdout main;
    error_log /dev/stderr;
  }
}
```

- `listen 80` : 80번 포트에서 HTTP 요청을 대기
- `server_name echo.jpub.local` : 요청 호스트 이름을 정의. 클라이언트가 `Host: echo.jpub.local`로 지정해서 보낸 요청에만 이 `server` 블록이 반응 — 이걸 **가상 호스트**라고 부르며, 같은 nginx 하나로 여러 도메인을 구분해서 다르게 처리할 수 있게 해줌
- `location /` : 모든 요청 경로를 리버스 프록시로 전송. `/`는 모든 경로를 뜻하므로, 이 서버로 들어오는 요청은 전부 이 블록을 탐

> 📌 `upstream.conf`가 "누구한테 넘길지" 대상을 정의한 파일이라면, `echo.conf`는 "언제, 어떤 요청을 그 대상한테 넘길지"를 정의하는 파일이다.

**5) nginx의 Dockerfile 작성**

위치: `./nginx/Dockerfile`

```dockerfile
FROM nginx:1.25.1
COPY ./etc/nginx/conf.d/* /etc/nginx/conf.d/
```

nginx 자체를 처음부터 새로 만드는 게 아니라, 이미 완성된 nginx 이미지를 베이스로 삼고 그 위에 설정 파일만 얹는 방식 — echo 실습 때 `FROM golang:1.21.6`으로 Go 런타임을 가져온 것과 같은 패턴.

- `FROM nginx:1.25.1` : nginx 실행 파일, 기본 설정, 필요한 라이브러리가 이미 다 갖춰진 공식 이미지를 베이스로 사용
- `COPY ./etc/nginx/conf.d/* /etc/nginx/conf.d/` : 방금 만든 `upstream.conf`, `echo.conf`를 이미지 내부의 같은 경로로 복사. 원래 있던 기본 `default.conf`는 파일명이 다르므로 덮어써지지 않고 나란히 추가되는데, nginx는 `conf.d/*.conf`를 전부 로드하므로 나중에 `default.conf`와 설정이 충돌할 수 있어 실무에서는 `default.conf`를 지우거나 덮어쓰는 경우도 있음

(호스트의 `nginx/etc/nginx/conf.d/` 폴더 구조를 컨테이너 내부 경로와 똑같이 맞춰뒀기 때문에, `COPY` 경로가 직관적으로 대응된다.)

**6) compose.yaml 완성**

지금까지 준비한 요소(echo 이미지, nginx Dockerfile, upstream/echo.conf)를 하나의 파일로 묶어서 "이 두 컨테이너를 세트로 띄워라"고 정의한다.

```yaml
services:
  echo:
    image: ghcr.io/jpubdocker/echo:v0.1.0

  nginx:
    # nginx 디렉터리를 컨텍스트로 이미지 빌드
    build: ./nginx
    # 컨테이너 간 통신을 위해 echo 컨테이너 의존성 추가
    depends_on:
      - echo
    # 호스트 9000 포트를 nginx의 80 포트로 포워딩
    ports:
      - "9000:80"
```

- `echo` 서비스 : 이미 완성된 이미지를 그대로 받아와서 실행. `ports` 설정이 없으므로 호스트에서 echo로 직접 접속하는 문은 열리지 않음 — nginx가 컨테이너 간 통신으로만 접근하는 구조라 호스트 노출이 불필요
- `nginx` 서비스의 `build: ./nginx` : 미리 만들어둔 이미지를 받아오는 게 아니라, `./nginx` 폴더(Dockerfile + conf.d 파일들이 있는 폴더)를 빌드 컨텍스트로 삼아 직접 이미지를 빌드 (`docker image build -t ... ./nginx`를 컴포즈가 대신 실행해주는 셈)
- `depends_on: - echo` : echo가 먼저 뜬 다음 nginx가 뜨도록 순서를 보장. nginx가 뜨자마자 echo로 프록시를 시도하는데, echo가 아직 없으면 연결이 실패할 수 있어 필요한 의존 관계 선언
- `ports: - "9000:80"` : 호스트 9000번을 nginx 컨테이너 내부 80번(`echo.conf`의 `listen 80;`)에 매핑. 이 구조 전체에서 호스트로 열리는 문은 이 한 곳뿐

**7) 실행 및 확인**

```powershell
docker compose up -d
# [+] up 2/2
#  ✔ Container nginx-echo-echo-1   Started
#  ✔ Container nginx-echo-nginx-1  Started
```

9000번 포트에서 HTTP 요청을 대기하고 있으므로, curl로 요청을 보낸다.

```bash
# 책 속 코드 (리눅스/맥 기준)
curl -H 'Host: echo.jpub.local' http://localhost:9000
```

> ⚠️ **PowerShell 트러블슈팅**: PowerShell의 `curl`은 진짜 curl이 아니라 `Invoke-WebRequest`의 별칭. 리눅스/맥 curl과 옵션 문법이 달라서 `-H 'Key: Value'` 형식을 못 알아듣고, 자기 방식인 `-Headers`(해시테이블)를 요구하며 에러가 발생한다.
>
> 해결 방법 두 가지:
> 1. PowerShell 방식으로 고쳐 쓰기 — `curl -Headers @{Host='echo.jpub.local'} http://localhost:9000`
> 2. 진짜 `curl.exe`를 직접 호출 — Windows 10/11에는 실제 curl.exe가 내장되어 있어서, `curl.exe -H 'Host: echo.jpub.local' http://localhost:9000`처럼 부르면 책 예제 문법 그대로 사용 가능

```
Hello Container!!
```

정상적으로 nginx가 리버스 프록시로 echo 컨테이너까지 요청을 넘기고, 응답이 돌아온 것을 확인.
