# Chapter 1. 도커 개념 이해하기

## 1. 컨테이너형 가상화란

> 가상화 기술 비교: 호스트 OS형 vs 컨테이너형 (단독주택 vs 아파트 비유) — 참고용 다이어그램은 Notion 원본 페이지에서 확인.

호스트 OS형 가상화와 컨테이너형 가상화는 **가상화하는 대상의 단위**가 다르다.

| 구분 | 호스트 OS형 가상화 | 컨테이너형 가상화 |
| --- | --- | --- |
| 대표 예 | VMware Workstation/Fusion, VirtualBox, Parallels Desktop | Docker |
| 커널 | 인스턴스마다 각자 Guest OS + 커널을 통째로 실행 | 호스트 커널 하나를 여러 컨테이너가 공유 |
| 격리 단위 | 가상 머신 (하드웨어 수준까지 가상화) | 프로세스 (커널 위에서 격리된 실행 환경) |
| 무게 | 무거움 (Guest OS 전체를 매번 부팅) | 가벼움 (OS 부팅 과정 없이 즉시 실행) |
| 비유 | 단독주택 여러 채를 각각 새로 짓는 것 | 아파트 한 채 안에 여러 세대(방)를 두는 것 |

핵심은 컨테이너형이 **커널을 공유**한다는 점이다. 앞서 배운 "이미지 레이어"도 같은 맥락 — 아파트가 공용 골조(호스트 커널) 위에 세대별 구획(컨테이너)만 얹듯이, 이미지도 공용 베이스 레이어 위에 얇은 레이어만 얹어 재사용한다.

---

## 2. 컨테이너 사용 사례와 장단점

로컬 개발 환경에서 Apache httpd, nginx 같은 웹 서버 소프트웨어를 설치 없이 컨테이너로 즉시 띄웠다가 바로 지울 수 있다는 것이 대표적인 사용 사례다.

| 구분 | 내용 |
| --- | --- |
| 장점 — 이식성 | 어떤 환경(개발자 PC, CI, 운영 서버)에서도 동일하게 동작 |
| 장점 — 애플리케이션 플랫폼 | 앱 실행에 필요한 실행환경(런타임, 라이브러리, 설정)까지 이미지 하나에 통째로 패키징해서 배포/실행 가능 |
| 단점 — 리눅스 커널 의존 | 컨테이너는 리눅스 커널 위에서 동작. Windows/Mac에서는 경량 가상 리눅스 커널(WSL2 등)을 거쳐 실행됨 |

---

## 3. 도커의 구성 요소

| 구성 요소 | 역할 |
| --- | --- |
| dockerd (Docker 데몬) | 이미지 빌드, 컨테이너 실행 등 실제 핵심 작업을 수행하는 백그라운드 프로세스(서버) |
| Docker REST API | dockerd가 외부에 노출하는 명령 인터페이스 |
| Docker CLI | 터미널에서 직접 입력하는 `docker` 명령어. 내부적으로 REST API를 호출해 dockerd에 작업을 요청 |

실습에서 쳤던 `docker image build`, `docker container run` 명령은 모두 **CLI → REST API → dockerd** 순서로 전달되어 실행된다.

---

## 4. 도커의 동작 방식 — Dockerfile 개요

실행 시점을 기준으로 정리하면 다음과 같다. (각 명령어의 상세 사용법은 Chapter 2에서 이어서 다룬다.)

| 명령어 | 실행 시점 | 동작 |
| --- | --- | --- |
| FROM | 빌드 시 | 베이스 이미지 지정 (모든 레이어의 시작점) |
| WORKDIR | 빌드 시 | 이후 명령이 실행될 기본 디렉터리 설정 |
| COPY | 빌드 시 | 호스트 파일을 이미지 내부로 복사 → 새 레이어 생성 |
| RUN | 빌드 시 | 이미지 내부에서 명령 실행, 결과를 새 레이어로 저장 |
| CMD | 컨테이너 실행 시 (기본값) | 컨테이너 시작 시 실행할 기본 명령. `docker container run` 뒤 인수로 통째로 덮어쓰기 가능 |
| ENTRYPOINT | 컨테이너 실행 시 (고정) | 항상 실행될 명령을 고정. 뒤에 오는 인수는 덮어쓰지 않고 뒤에 이어붙음 |
| LABEL | 빌드 시 | 이미지에 메타데이터(라벨) 부여 — 버전, 소스 URL 등 |

핵심: FROM/WORKDIR/COPY/RUN은 이미지를 **만드는(빌드)** 시점에, CMD/ENTRYPOINT는 이미지를 **실행하는(런타임)** 시점에 동작한다는 차이가 있다.

---

## 5. 왜 컨테이너 기술을 사용하는가 — 4가지 이유

### 1) 재현성 확보 (Immutable Infrastructure)

기존 IaC(Infrastructure as Code) 방식은 서버 프로비저닝 코드(예: `nvm install`)를 실행할 때, 실행 시점의 외부 상태에 따라 결과가 달라질 수 있어 같은 코드를 실행해도 서버마다 다른 상태가 되는 문제가 있다 — 즉 "재현성"이 깨진다.

Immutable Infrastructure는 특정 시점의 서버 상태를 이미지로 통째로 저장해두고 그대로 복제하는 방식이다. 실행할 때마다 항상 의도한 대로 설정된 서버를 그대로 재현할 수 있다. Docker 이미지가 정확히 이 방식이다 — Dockerfile로 한 번 빌드된 이미지는 내용이 고정되어 언제 어디서 실행해도 동일한 결과를 보장한다.

### 2) 구성 관리의 용이성

Docker Compose는 여러 컨테이너(서비스)를 하나의 YAML 파일로 정의하고, `depends_on`으로 시작 순서까지 관리할 수 있게 해준다. 컨테이너 수가 더 늘어나 여러 서버(노드)에 걸쳐 운영해야 할 때는 Docker Swarm이나 Kubernetes 같은 오케스트레이션 도구가 부하 분산과 무중단 배포를 지원한다.

> ⚠️ **주의**: 이미지 자체는 OCI 표준을 따르므로 Swarm/Kubernetes 어디서든 실행 가능(이식성 O)하지만, 오케스트레이션 설정 파일(Compose vs Swarm vs Kubernetes manifest)은 서로 호환되지 않는다. "이미지의 이식성"과 "배포 설정의 호환성"은 별개의 문제다. 현재는 CNCF가 주도하고 여러 벤더가 함께 참여하는 Kubernetes가 사실상 표준으로 자리잡았다.

### 3) 높은 이식성

컨테이너 이미지는 파일 시스템 단위로 패키징되어 어떤 환경에서든 동일하게 실행 가능하다. GitHub Actions, CircleCI 같은 CI 서비스에서도 컨테이너를 그대로 사용할 수 있고, 빌드에 쓴 것과 동일한 이미지로 E2E(End-to-End) 테스트까지 수행할 수 있다 — 빌드 환경과 테스트 환경, 배포 환경을 일치시킬 수 있다는 의미다.

### 4) 컨테이너 기반 개발의 효율성

서버리스 플랫폼(Google Cloud Run, AWS Lambda 등)은 기존에 개발자가 직접 관리해야 했던 가상 서버(VM)의 운영을 클라우드 매니지드 서비스로 캡슐화한다. 그 결과 개발자는 가상 서버 운영 부담에서 벗어나고, 컨테이너의 구성(이미지, 실행 옵션)에만 집중할 수 있게 된다.

---

## 6. YAML 파일의 정의와 역할

YAML(YAML Ain't Markup Language)은 들여쓰기와 콜론(`:`)으로 계층 구조를 표현하는 설정 파일 포맷이다.

- 규칙: 같은 계층은 같은 들여쓰기, 리스트 항목은 대시(`-`) 뒤에 반드시 공백 하나
- 쓰이는 곳: Docker Compose(`compose.yaml`), Kubernetes manifest, GitHub Actions workflow, 그리고 Spring Boot의 `application.yml`(환경별 설정을 Profile로 분리할 때도 사용)

YAML은 도커에 국한된 문법이 아니라, 여러 시스템의 설정을 사람이 읽기 쉬운 형태로 기술하기 위해 폭넓게 채택된 공통 포맷이다.

---

## 7. 로컬 환경에 도커 설치하기 (Windows)

Windows에서 Docker Desktop은 내부적으로 WSL2(Windows Subsystem for Linux 2)를 백엔드로 사용해 리눅스 커널을 구동한다.

`.wslconfig` 파일은 WSL2 전체에 적용되는 설정 파일(사용자 홈 디렉터리에 위치)로, 다음 항목을 지정할 수 있다.

| 항목 | 역할 |
| --- | --- |
| memory | WSL2에 할당할 최대 메모리 |
| processors | 할당할 CPU 코어 수 |
| swap | 스왑 메모리 크기 |
| localhostForwarding | WSL2 내부 포트를 Windows의 localhost로 포워딩할지 여부 |
